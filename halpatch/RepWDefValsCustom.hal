
global
procedure DaySalesRClassReportDefaults(Integer wn)
BEGIN
  record RcVc RepSpec;
  
  DeselectWindow(wn,false);
  GetWindowRecord(wn,RepSpec);
  ReportDefaults(RepSpec,"DaySalesRClass");  
  RepSpec.RegDate = CurrentDate;
  PutWindowRecord(wn,RepSpec);
  SelectWindow(wn);
  RETURN;
END;

